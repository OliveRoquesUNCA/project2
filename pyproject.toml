[tool.poetry]
name = "proj2"
version = "0.1.0"
description = "Giphy CLI"
authors = ["Olive Roques"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
requests = "^2.31.0"
click = "^8.1.7"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
